# @rxova/example

A placeholder package. Rename it (directory, `name`, `repository.directory`) or copy it for each new
package.

```sh
pnpm add @rxova/example
```

```ts
import { greet } from '@rxova/example';

greet('Ada'); // 'Hello, Ada.'
```
