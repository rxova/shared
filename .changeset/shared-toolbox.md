---
'@rxova/toolbox': minor
---

Add `@rxova/toolbox`, a set of dependency-free runtime helpers:

- object predicates and reflection that never throws;
- `isError` and `errorMessage`;
- `shallowEqual`;
- `isDevelopment` and `createDevWarner`;
- `canUseDOM`, `deepFreeze` and `clamp`;
- `useIsomorphicLayoutEffect` under `@rxova/toolbox/react`.
